apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tracefinity.fullname" . }}-test-connection"
  labels:
    {{- include "tracefinity.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tracefinity.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
