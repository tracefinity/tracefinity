apiVersion: v1
kind: Service
metadata:
  name: {{ include "tracefinity.fullname" . }}
  labels:
    {{- include "tracefinity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tracefinity.selectorLabels" . | nindent 4 }}
